# Verify: Physical & Geometric Object Feature Extractor · spec 0003 · updated 2026-09-19
_Steps derived from spec 0003 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones._

## Commands
* [x] `python3 -c "from testing.feature_extractor import CANONICAL_FEATURE_NAMES; assert len(CANONICAL_FEATURE_NAMES) == 17; assert CANONICAL_FEATURE_NAMES[0] == 'area_km2' and CANONICAL_FEATURE_NAMES[-1] == 'touches_suspect_vessel'"` → Canonical feature vector ordering contains exactly 17 defined feature keys in strict order → AC-1, Value Sourcing: Assemble matrix
* [x] `python3 -c "from testing.feature_extractor import compute_polygon_geometric_features; pts=[(10,10),(510,10),(510,20),(10,20)]; geom=compute_polygon_geometric_features(pts, 5000, (10,10,510,20), 0.05, 10.2); assert geom['elongation'] > 15.0 and geom['circularity'] < 0.20 and 1.0 <= geom['fractal_dimension'] <= 2.0"` → Thin ribbon candidate polygon produces high elongation, low circularity, and valid fractal dimension → AC-1, Value Sourcing: Extract shape geometry, Extract complexity, Extract fractal dimension
* [x] `python3 -c "from testing.feature_extractor import compute_polygon_geometric_features; import numpy as np; angles=np.linspace(0,2*np.pi,64,endpoint=False); pts=[(int(100*np.cos(a)+150),int(100*np.sin(a)+150)) for a in angles]; geom=compute_polygon_geometric_features(pts, int(np.pi*100*100), (50,50,250,250), 0.0314, 0.628); assert 0.90 <= geom['circularity'] <= 1.05 and 0.95 <= geom['elongation'] <= 1.25"` → Circular candidate polygon yields circularity and elongation near 1.0 → AC-1, Value Sourcing: Extract shape geometry, Extract complexity
* [x] `python3 -c "from testing.feature_extractor import compute_boundary_gradient_sharpness; import numpy as np, scipy.ndimage; vv=np.full((100,100),-15.0,dtype=np.float32); vv[30:70,30:70]=-25.0; mag=np.hypot(scipy.ndimage.sobel(vv,1), scipy.ndimage.sobel(vv,0)); msk=np.zeros((100,100),dtype=bool); msk[30:70,30:70]=True; g_mean, g_max = compute_boundary_gradient_sharpness(msk, mag, np.ones((100,100),dtype=bool)); assert g_mean > 5.0 and g_max > 10.0"` → Precomputed Sobel gradient sampling along 3 px dilated boundary detects sharp interfacial edges → AC-3, Value Sourcing: Compute boundary sharpness
* [x] `python3 -c "from testing.feature_extractor import extract_candidate_features, CandidateDarkSpot; from testing.vessel_masking import VesselTarget; import numpy as np; sar=np.full((100,100,2),-16.0,dtype=np.float32); sar[30:50,30:50,0]=-23.0; sar[30:50,30:50,1]=-30.0; cand=CandidateDarkSpot('spot_001',1,400,0.04,(30,30,50,50),(0,0,0,0),{},[(30,30),(50,30),(50,50),(30,50)],(40.0,40.0),(0,0),-23.0,-23.0,-30.0,7.0,touches_suspect_vessel=True); vessel=VesselTarget('vessel_001',52.0,40.0,None,None,5.0,-5.0,10,is_suspect_source=True); res=extract_candidate_features([cand], np.ones((100,100),dtype=np.int32), sar, np.full((100,100),7.0,dtype=np.float32), {'pixel_size_x':10.0,'pixel_size_y':10.0}, [vessel]); f=res.candidate_features[0]; assert f.touches_suspect_vessel==1.0 and f.distance_to_nearest_vessel_m==120.0 and f.vv_vh_ratio==7.0"` → Dual polarimetric contrast, texture std, and suspect vessel proximity are accurately extracted and linked → AC-2, AC-4, Value Sourcing: Re-use candidate backscatter, Compute texture & polarimetry, Re-use vessel context
* [x] `python3 -c "from testing.feature_extractor import extract_candidate_features, CandidateDarkSpot; import numpy as np; cand=CandidateDarkSpot('spot_001',1,400,0.04,(10,10,30,30),(0,0,0,0),{},[(10,10),(30,10),(30,30),(10,30)],(20.0,20.0),(0,0),-20.0,-20.0,-25.0,3.0); res=extract_candidate_features([cand], np.ones((50,50),dtype=np.int32), np.full((50,50,2),-20.0,dtype=np.float32), np.full((50,50),3.0,dtype=np.float32), {'pixel_size_x':10.0,'pixel_size_y':10.0}, vessels=[]); f=res.candidate_features[0]; assert f.distance_to_nearest_vessel_m==10000.0 and f.touches_suspect_vessel==0.0"` → Missing vessel scenes cleanly impute default 10,000 meters distance and 0.0 touches flag without NaN → AC-4, Value Sourcing: Re-use vessel context
* [x] `python3 -c "from testing.feature_extractor import extract_candidate_features; import numpy as np; res=extract_candidate_features([], np.zeros((10,10),dtype=np.int32), np.zeros((10,10,2),dtype=np.float32), np.zeros((10,10),dtype=np.float32), {}); assert res.candidate_count==0 and res.feature_matrix.shape==(0,17)"` → Empty candidate scene gracefully yields 0 by 17 empty feature matrix without crashing → AC-5, Value Sourcing: Assemble matrix
* [x] `python3 -c "from testing.feature_extractor import export_features_tabular, FeatureExtractionResult, CandidateObjectFeatures, CANONICAL_FEATURE_NAMES; import numpy as np, tempfile, csv, os; feat=CandidateObjectFeatures('spot_001',0.01,0.4,1.2,0.85,4.0,1.05,0.75,-22.0,-28.0,-25.0,-6.0,1.1,4.5,3.2,7.8,500.0,0.0); res=FeatureExtractionResult('test',1,CANONICAL_FEATURE_NAMES,np.array([feat.to_numpy_vector()]),[feat]); td=tempfile.mkdtemp(); p=os.path.join(td,'feat.csv'); df=export_features_tabular(res, p); rows=list(csv.reader(open(p))); assert rows[0]==['candidate_id']+CANONICAL_FEATURE_NAMES and len(rows)==2"` → Exports extracted features to verified CSV table and pandas DataFrame with exact canonical schema → AC-5, Value Sourcing: Assemble matrix
* [x] `pytest -q testing/test_feature_extractor.py` → Full automated test suite passes 11 of 11 tests covering all unit and real scene integration requirements → AC-1, AC-2, AC-3, AC-4, AC-5

## Acceptance-criteria coverage
* AC-1 (Geometric morphology and shape descriptor extraction): covered by commands 1, 2, 3, 9
* AC-2 (Physical radar backscatter and polarimetric contrast extraction): covered by commands 5, 9
* AC-3 (Interfacial boundary gradient sharpness calculation): covered by commands 4, 9
* AC-4 (Spatial contextual vessel proximity features): covered by commands 5, 6, 9
* AC-5 (Dual container encapsulation and tabular matrix export): covered by commands 1, 7, 8, 9
