# Verify: Adaptive dark spot candidate segmentation · spec 0002 · updated 2026-09-19
_Steps derived from spec 0002 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones._

## Commands
* [x] `python3 -c "from testing.segmentation import compute_adaptive_ocean_background; import numpy as np; vv=np.full((100,100),-15.0,dtype=np.float32); valid=np.ones((100,100),dtype=bool); bg=compute_adaptive_ocean_background(vv, valid, window_size=21); assert np.allclose(bg, -15.0)"` → Moving window average accurately estimates ambient ocean clutter across valid pixels → AC-1, Value Sourcing: local_ocean_mean
* [x] `python3 -c "from testing.segmentation import compute_adaptive_ocean_background; import numpy as np; vv=np.full((100,100),-16.0,dtype=np.float32); valid=np.zeros((100,100),dtype=bool); valid[10:15,10:15]=True; bg=compute_adaptive_ocean_background(vv, valid, window_size=51, min_samples=100); assert np.allclose(bg[valid], -16.0)"` → Coastal boundary zone with fewer than 100 samples smoothly falls back to global ocean median → AC-1, Value Sourcing: local_ocean_mean fallback
* [x] `python3 -c "from testing.segmentation import compute_damping_ratio_map; import numpy as np; vv=np.full((100,100),-15.0,dtype=np.float32); vv[40:60,40:60]=-25.0; valid=np.ones((100,100),dtype=bool); dr=compute_damping_ratio_map(vv, np.full((100,100),-15.0,dtype=np.float32), valid); assert np.isclose(dr[50,50], 10.0) and dr[0,0]==0.0"` → Relative damping contrast computes positive decibels inside slick and strictly zero outside valid ocean → AC-2, Value Sourcing: damping_ratio_map
* [x] `python3 -c "from testing.segmentation import segment_candidate_dark_spots; import numpy as np; sar=np.full((100,100,2),-15.0,dtype=np.float32); sar[30:70,30:70,0]=-26.0; meta={'pixel_size_x':10.0,'pixel_size_y':10.0,'transform':[10.0,0.0,500000.0,0.0,-10.0,3000000.0],'crs':'EPSG:32640'}; res=segment_candidate_dark_spots(sar, meta, min_damping_db=2.5, max_backscatter_db=-20.0); assert res.candidate_count==1 and res.candidates[0].pixel_count==1600"` → Isolates candidate dark spot meeting minimum damping ratio and maximum backscatter ceiling → AC-2, AC-3, Value Sourcing: Initial candidate mask, candidate_mask
* [x] `python3 -c "from testing.segmentation import segment_candidate_dark_spots; import numpy as np; sar=np.full((100,100,2),-15.0,dtype=np.float32); sar[10:12,10:12,0]=-26.0; sar[40:60,40:48,0]=-26.0; sar[40:60,50:60,0]=-26.0; meta={'pixel_size_x':10.0,'pixel_size_y':10.0,'transform':[10.0,0.0,500000.0,0.0,-10.0,3000000.0],'crs':'EPSG:32640'}; res=segment_candidate_dark_spots(sar, meta); assert res.candidate_count==1 and res.candidates[0].pixel_count > 300"` → Bridges interior gaps with 3x3 closing, connects diagonal neighbors with 8-connectivity, and prunes speckle under 30 px → AC-3, Value Sourcing: Extract components
* [x] `python3 -c "from testing.segmentation import segment_candidate_dark_spots; from testing.vessel_masking import VesselTarget; import numpy as np; sar=np.full((100,100,2),-15.0,dtype=np.float32); sar[40:60,40:60,0]=-25.0; meta={'pixel_size_x':10.0,'pixel_size_y':10.0,'transform':[10.0,0.0,500000.0,0.0,-10.0,3000000.0],'crs':'EPSG:32640'}; vessels=[VesselTarget('vessel_001',50.0,35.0,15.0,20.0,30.0,-5.0,10,is_suspect_source=True)]; res=segment_candidate_dark_spots(sar, meta, vessels=vessels); assert res.candidate_count==1 and res.candidates[0].touches_suspect_vessel"` → Vessel masking integration links suspect vessels within 35 pixels and flags discharging slicks → AC-4, Value Sourcing: Vessel shadow integration, Link vessel context
* [x] `python3 -c "from testing.segmentation import segment_candidate_dark_spots, export_candidate_geojson; import numpy as np, tempfile, os, json; sar=np.full((100,100,2),-15.0,dtype=np.float32); sar[40:60,40:60,0]=-25.0; meta={'pixel_size_x':10.0,'pixel_size_y':10.0,'transform':[10.0,0.0,500000.0,0.0,-10.0,3000000.0],'crs':'EPSG:32640'}; res=segment_candidate_dark_spots(sar, meta); td=tempfile.mkdtemp(); p=os.path.join(td,'cands.geojson'); export_candidate_geojson(res.candidates, meta, p); feat=json.load(open(p))['features'][0]; assert feat['geometry']['type']=='Polygon' and feat['properties']['area_km2'] > 0"` → Generates valid GeoJSON Polygon with geodesic area in square kilometers via WGS84 ellipsoid → AC-5, Value Sourcing: Calculate geodesic area, Calculate geographic coordinates
* [x] `pytest -q testing/test_segmentation.py` → Full automated unit and integration test suite passes 11 of 11 tests covering all acceptance criteria → AC-1, AC-2, AC-3, AC-4, AC-5

## Acceptance-criteria coverage
* AC-1 (Moving window ocean background and coastal fallback): covered by commands 1, 2, 8
* AC-2 (Relative damping thresholding and non ocean masking): covered by commands 3, 4, 8
* AC-3 (Morphological closing, 8-connectivity labeling, and speckle pruning): covered by commands 4, 5, 8
* AC-4 (Vessel masking integration and suspect ship proximity linkage): covered by commands 6, 8
* AC-5 (Geodesic area calculation, vector polygonization, and GeoJSON export): covered by commands 7, 8
